parameters:
  shouldExecute: false
  
steps:
- ${{ if parameters.shouldExecute == true }}:
  - task: Bash@3
    displayName: 'Install bash'
    inputs:
      targetType: 'inline'
      script: |
        echo "I'm alive!"
