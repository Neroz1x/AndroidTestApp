parameters:
  shouldExecute: false
  
steps:
- ${{ if eq(parameters.shouldExecute, 'true') }}:
  - task: Bash@3
    displayName: 'Install bash'
    inputs:
      targetType: 'inline'
      script: |
        echo "I'm alive!"
